/**
 * Tipos auxiliares de UI restantes do Team Link.
 *
 * Os tipos de domínio (Profile, Project, etc.) agora vivem em `@/types/database`
 * e refletem o schema real do Supabase. Mantemos este arquivo apenas para
 * tipos puramente visuais — atualmente vazio porque os tipos legados de mock
 * foram removidos junto da integração com Supabase.
 */

export {}
